name: trigger-block-alias
on:
  push: &trigger
    branches: [main]
    paths-ignore:
      - "**.md"
  pull_request_target: *trigger
permissions: {}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
