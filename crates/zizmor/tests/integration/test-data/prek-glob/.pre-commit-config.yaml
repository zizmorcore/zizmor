files: "\\.(yaml|yml)$"
exclude:
  glob: "**/snapshots/**"
repos:
  - repo: https://github.com/example/hooks
    rev: v1.0.0
    hooks:
      - id: example
        files:
          glob:
            - "**/*.yaml"
            - "**/*.yml"
        exclude: "^vendor/"
