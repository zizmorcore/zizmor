on:
  workflow_call:
    # OK: explicitly declares the secrets it accepts.
    secrets:
      my-secret:
        required: true

name: secrets-inherit-callee-ok

permissions: {}

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from a reusable workflow"
