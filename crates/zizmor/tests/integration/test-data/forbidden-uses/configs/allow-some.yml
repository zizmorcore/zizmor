rules:
  unpinned-uses:
    config:
      policies:
        # just to make unrelated findings go away
        "*": ref-pin

  forbidden-uses:
    config:
      allow:
        - "actions/checkout"
        - "pypa/gh-action-pypi-publish"
