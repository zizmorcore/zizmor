on:
  push:

name: self-hosted-runner-group

permissions: {}

jobs:
  whops:
    name: whops
    runs-on:
      group: ubuntu-runners

    steps:
      - run: echo \"hello from a self-hosted runner\"
