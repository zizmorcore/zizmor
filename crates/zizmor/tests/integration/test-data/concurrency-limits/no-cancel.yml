name: Workflow without cancel-in-progress
on: push
permissions: {}

concurrency: group

jobs:
  job:
    name: some-job
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
    - name: 1-ok
      run: echo ok
