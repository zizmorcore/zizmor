name: Workflow with a job without cancel-in-progress
on: push
permissions: {}

jobs:
  job:
    name: some-job
    runs-on: ubuntu-latest
    concurrency: group
    steps:
    - name: 1-ok
      run: echo ok
