---
name: Test
description: |
  Composite action for testing
inputs:
  shell:
    default: bash --noprofile {0}
    required: false
runs:
  using: composite
  steps:
    - run: |
        echo "Path is: $PATH"
      shell: ${{ inputs.shell }}
