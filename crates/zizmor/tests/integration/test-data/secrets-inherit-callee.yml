on:
  workflow_call:
    # NOT OK: forces every caller to over-scope by handing over all secrets.
    secrets: inherit

name: secrets-inherit-callee

permissions: {}

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from a reusable workflow"
