---
name: Test
description: |
  Composite action for testing
inputs:
  shell:
    description: The shell to use
    default: bash --noprofile {0}
    required: false
runs:
  using: composite
  steps:
    - run: |
        echo "Path is: $PATH"
      shell: ${{ inputs.shell }}
